

## Remove Moove — Fire from Homepage Carousel

The Moove — Fire entry (lines 62-68) in `src/components/WorkSection.tsx` needs to be removed from the `projects` array. It will remain on the Selected Works page (`/work`) only.

### Change

**`src/components/WorkSection.tsx`** — Delete the Moove — Fire object from the `projects` array (lines 62-68), keeping the other 6 projects intact.

